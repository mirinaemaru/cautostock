# Trading System - 환경 변수 설정 예제
#
# 사용법:
#   cp .env.example .env.local
#   cp .env.example .env.prod
#
# 그 다음 각 파일을 환경에 맞게 수정하세요.

# ==================== Java 설정 ====================

# Java Home 경로
JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64

# JVM 옵션
JVM_OPTS="-Xms2g -Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=200"

# ==================== 데이터베이스 설정 ====================

# 데이터베이스 비밀번호
DB_PASSWORD=your_secure_password_here

# 데이터베이스 URL (기본값: localhost:3306)
# DB_URL=jdbc:mariadb://localhost:3306/trading_mvp

# 데이터베이스 사용자 (기본값: trading_user)
# DB_USER=trading_user

# ==================== KIS API 설정 (PAPER - 모의투자) ====================

# KIS PAPER APP KEY
KIS_PAPER_APP_KEY=your_paper_app_key_here

# KIS PAPER APP SECRET
KIS_PAPER_APP_SECRET=your_paper_app_secret_here

# KIS PAPER 계좌번호 (앞 8자리)
KIS_PAPER_ACCOUNT_NO=50000000

# KIS PAPER 계좌상품코드 (2자리, 보통 01)
KIS_PAPER_ACCOUNT_PRODUCT=01

# ==================== KIS API 설정 (LIVE - 실전투자) ====================
# ⚠️ 주의: LIVE 모드는 실제 거래를 실행합니다!

# KIS LIVE APP KEY (사용 시 주석 해제)
# KIS_LIVE_APP_KEY=your_live_app_key_here

# KIS LIVE APP SECRET (사용 시 주석 해제)
# KIS_LIVE_APP_SECRET=your_live_app_secret_here

# ==================== 애플리케이션 설정 ====================

# Spring Profile (local, dev, prod)
SPRING_PROFILE=local

# 서버 포트 (기본값: 8080)
# SERVER_PORT=8080

# ==================== 배포 설정 ====================

# 배포 디렉토리 (기본값: $HOME/trading-system)
# DEPLOY_DIR=/opt/trading-system

# 테스트 실행 여부 (true/false, 기본값: true)
RUN_TESTS=true

# 자동 재시작 여부 (true/false, 기본값: false)
AUTO_RESTART=false

# ==================== 로깅 설정 ====================

# 로그 레벨 (DEBUG, INFO, WARN, ERROR)
# LOG_LEVEL=INFO

# 로그 파일 경로 (기본값: logs/application.log)
# LOG_FILE_PATH=/var/log/trading-system/application.log
